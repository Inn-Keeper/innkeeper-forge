# Required for public + private repo sync (Inn-Keeper PAT with `repo` scope)
GITHUB_TOKEN=

# Required in production for on-demand portfolio refresh
REVALIDATION_SECRET=your-secret-here

# Used by `pnpm refresh` locally
PORTFOLIO_URL=http://localhost:3000
